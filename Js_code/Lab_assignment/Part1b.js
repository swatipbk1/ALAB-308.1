/*Part 1 Problem(b)-Check if the first number is larger than the last. 
Cache the result in a variable.Check if the first number is larger than the last.
 Cache the result in a variable.*/

let a=prompt("enter the numbers to compare");
let b=prompt("enter the numbers to compare");
if (a>=b) {
  console.log("A is the largest of the two");
}
else {
    console.log("B is the largest of the two");
}